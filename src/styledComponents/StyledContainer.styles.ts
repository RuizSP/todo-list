import styled from "styled-components";

// Componente estilizado para a div
export const StyledContainer = styled.div`
    display: flex;
    flex-direction: row;
    gap: 0.25rem;
    justify-content:center;
    margin-top: -26px;
`;